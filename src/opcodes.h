#ifndef OPCODES_H
#define OPCODES_H

// Enum for instruction opcodes and other token values
typedef enum {
    LOADI, LOAD, STORE, ADD, SUB, MULT, LSHIFT, RSHIFT, OUTPUT, NOP
} OpcodeType;

#endif // OPCODES_H
